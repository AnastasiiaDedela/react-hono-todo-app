To install dependencies:

```sh
bun install
```

To run server && client:

```sh
bun run dev
```

To run server:

```sh
bun run server
```

To run client:

```sh
bun run client
```

open http://localhost:5000
